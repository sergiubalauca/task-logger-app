export interface LoginModel {
    email: string;
    password: string;
}
export interface RegisterModel {
    email: string;
    password: string;
    username: string;
}

export interface LogOutModel {
    email: string;
}
